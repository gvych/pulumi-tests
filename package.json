{
    "name": "pulumi-tests",
    "main": "index.ts",
    "scripts": {
        "deploy-and-test": "ts-node automation.ts",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "@types/node": "^18",
        "typescript": "^5.0.0",
        "ts-node": "^10.9.2",
        "vitest": "^1.0.4"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3.113.0",
        "@pulumi/docker": "^4.0.0",
        "axios": "^1.6.0"
    }
}
